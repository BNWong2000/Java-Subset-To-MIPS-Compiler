int i;
int j;
boolean k;

int bar(int i, int k){
    int l;
    return foo();
}
void sss(){
    return;
}

main(){
    // prints("asdf");
    // prints("as");
    int i;
    // i = foo() + foo() + foo();
    bar(1, bar(foo(), foo()));
    // int i;
    // i = 0;
    // i = i + 1;
    // i = 1 + foo() + 1 + 1;
}

int foo(){
    return 1;
}

